function deepEqual (obOne, obTwo) {
  let output;
  arrOne = object.keys(obOne).sort();
  arrTwo = object.keys(obTwo).sort();

  if(typeof obOne == typeof obTwo == object 
     && arrOne != null) {
    if(arrOne.length != arrTwo.lenght 
       || JSON.stringify(arrOne) != JSON.stringify(arrTwo) ) {
      output = false;
    } else {
      for(let i = 0; i < arrOne.lenght;i++){
       
      }
    }
  }
}